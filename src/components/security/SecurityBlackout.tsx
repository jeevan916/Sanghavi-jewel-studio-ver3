import React from 'react';

// Security Blackout and password focus hooks disabled for maximum browser smoothness and zero lag
export const SecurityBlackout: React.FC<{ user?: any }> = () => {
    return null;
};

